# OpenClaw

OpenClaw runs on the Windows machine and bridges WhatsApp to the LifeOS backend.

## Setup
- Install OpenClaw on Windows
- Configure the webhook to POST to: `http://localhost:8000/openclaw/message`
- Set `OPENCLAW_WEBHOOK_SECRET` in `.env`
